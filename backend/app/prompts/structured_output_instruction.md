仅返回一个符合以下 JSON Schema 的 JSON 对象。不要输出 Markdown 代码块、前后说明、注释或 Schema 未定义的字段。使用 JSON 的双引号、true、false、null；数组和对象使用正确类型，不把它们编码成字符串。字符串中的换行必须按 JSON 规则转义。

遵循系统提示词的业务规则及字段间一致性要求。不得为了满足必填字段而编造事实；信息不足时使用规则允许的 null、空数组或明确说明不足的文本，枚举字段遵循系统规定的判断规则。

以下为输出结构约束；Schema 的表示中即使出现 Python 风格的引号或布尔值，你的响应也必须是标准 JSON：
{schema}
